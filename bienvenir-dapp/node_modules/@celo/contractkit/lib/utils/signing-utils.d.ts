import { Tx } from 'web3/eth/types';
import { EncodedTransaction } from 'web3/types';
export declare function signTransaction(txn: any, privateKey: string): Promise<EncodedTransaction>;
export declare function recoverTransaction(rawTx: string): [Tx, string];
